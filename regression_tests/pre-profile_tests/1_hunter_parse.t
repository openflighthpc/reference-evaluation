Flight hunter parses the data in its buffer

  $ flight hunter parse --auto --prefix node --start 00 | grep " node0"
  The table size exceeds the currently set width.Defaulting to vertical orientation.
