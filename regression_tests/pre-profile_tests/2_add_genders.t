Flight hunter can add give genders

  $ flight hunter modify-groups --add login,all "$(flight hunter list --plain | head -n 1 | awk '{print $5}')"
  Node(s) updated successfully:
  * (glob)
  * (glob)
  * (glob)
  * Label  * node00                                    * (esc) (glob)
  * (glob)
  * me     * al                                        * (esc) (glob)
  * IP     * *                             * (esc) (glob)
  * Groups * all, login                                * (esc) (glob)
  * (glob)
